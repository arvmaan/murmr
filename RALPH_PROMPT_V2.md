# murmer v2 — Voice-Triggered Prompt Templates + Adaptive Dictionary

Ralph loop prompt for implementing the differentiating features.

## Usage

```bash
cd /local/home/arvinmaa/murmer
/ralph-loop:ralph-loop "$(cat .ralph-prompt)" --max-iterations 40 --completion-promise DONE
```

---

## Prompt (copy to .ralph-prompt file)

See `.ralph-prompt` file in repo root.
